import './style.css'
import App from './App';
import React from 'react';
import ReactDOM from 'react-dom/client';
// Render your app component to the browser
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />)
